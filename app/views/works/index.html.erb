<div class="container">
  <%= table_for(:works) %>
</div>

<script>
  $(document).ready(function () {
    var $table = $('#works-table')

    $table.DataTable({
      processing: true,
      serverSide: true,
      ajax: $table.data('source'),
      pagingType: 'full_numbers',
      columns: <%= datatable_js_columns_for(:works) %>
    })
  })
</script>
